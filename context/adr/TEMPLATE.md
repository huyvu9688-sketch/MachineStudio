# ADR-NNNN: &lt;short decision title&gt;

- Status: Proposed | Accepted | Superseded by ADR-NNNN
- Date: &lt;YYYY-MM-DD decision date&gt;
- Related: &lt;roadmap phase / implementation-map unit / context file
  section, e.g. `context/architecture.md` "System Boundaries"&gt;

## Context

What problem or fork in the design forced this decision? State the
constraint or requirement that made the status quo (or the obvious
default) insufficient. Cite the specification section or prior discussion
that motivated it rather than restating it from memory — link
`context/project-overview.md`, `context/architecture.md`,
`context/roadmap.md`, or the relevant `context/progress-tracker.md` entry.

## Decision

State the decision in one or two sentences, then the specifics needed to
apply it consistently (what boundary it fixes, what it does *not* cover,
and any explicitly rejected alternative). Be concrete enough that a future
contributor can check a new piece of work against it without re-reading
the whole discussion.

## Consequences

- What this makes easier or guarantees going forward
- What this makes harder, or what it explicitly forecloses
- Any follow-on work, migration, or a future ADR this implies
- Alternatives considered and why they were rejected, if useful context

## Notes

Optional. Dated clarifications that do not change the decision itself.
Do not rewrite history here — see the immutability rule in
`context/adr/README.md`.
